{
  "name": "pointout",
  "private": true,
  "version": "0.1.0",
  "description": "PointOut – an open-source overlay widget that lets clients annotate a live demo website and send visual feedback straight to the developer.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/core",
    "dev:core": "npm run dev --workspace packages/core",
    "dev:server": "npm run dev --workspace packages/server"
  },
  "engines": {
    "node": ">=18"
  }
}
